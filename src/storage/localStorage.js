import projectName from './commonKey';

export const deviceTitleList = `deviceTitleList${projectName}`;// device table title list
export const userName = `userName${projectName}`; // online user
export const password = `password${projectName}`; // online pwd

